-- DROP DATABASE
DROP DATABASE IF EXISTS ecommerce_db;

-- CREATE DATABASE
CREATE DATABASE ecommerce_db;

-- //psql -U postgres
-- login first then take this code and paste into the command prompt to have it recreate the database that then neeeds to be seeded and then run the program itself (run npm i before just for good meassure)